package com.work.springboot.mongo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main implementation class Configuration and bootstrapping.
 * @author
 */
@SpringBootApplication
public class MyContactBootApplication {

	public static void main(String[] args) {
		SpringApplication.run(MyContactBootApplication.class, args);
	}
}